JWT_SECRET=replace-with-a-long-random-secret
# Comma-separated aliases that have admin dashboard access (case-insensitive)
ADMIN_ALIASES=youralias
FIRECRAWL_API_KEY=fc-your-firecrawl-api-key
# Optional: override this only if you self-host Firecrawl.
FIRECRAWL_API_URL=https://api.firecrawl.dev/v2
